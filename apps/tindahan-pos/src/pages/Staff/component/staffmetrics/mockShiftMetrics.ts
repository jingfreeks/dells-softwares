/**
 * TODO: replace with real numbers once shift tracking exists (see
 * lib/drawerFloat/drawerFloat.tsx — "there's no shift/drawer-count
 * concept in the data model yet"). On-shift presence, drawer variance,
 * and voids all require that feature; there's nothing in the schema
 * to compute them from today.
 */
export const MOCK_ON_SHIFT_NAME = "Maricel";
export const MOCK_ON_SHIFT_SINCE = "7:02 AM";
export const MOCK_DRAWER_VARIANCE = -40;
export const MOCK_DRAWER_VARIANCE_SHIFT_COUNT = 1;
export const MOCK_VOIDS_COUNT = 6;
export const MOCK_VOIDS_TOTAL = 412;
